module.exports = config => {
    // config.addPlugin(require('../heroicons'))
    config.addPlugin(require('../heroicons'), { className: 'heroicon' })
    // config.addPlugin(require('../heroicons'), { className: 'icon', errorOnMissing: true })
}